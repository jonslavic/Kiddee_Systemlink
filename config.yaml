# NI SystemLink connection — uses Auto configuration (same as LabVIEW Auto mode)
# No credentials required when running on the local SystemLink server.
systemlink:
  server_url: "http://localhost"   # kept for reference; Auto config handles this automatically

# Excel source file
excel:
  file_path: "C:\\Users\\12657\\OneDrive - Circuit Check Inc\\Kiddee\\7990497-01_Data_12months w pareto.xlsx"
  sheet_name: "testdata"

# Column mapping — matches the 'testdata' sheet exactly.
column_mapping:
  model:            "Model"
  serial_number:    "SerialNumber"
  status:           "TestResult"
  started_at:       "DateTime"
  step_name:        "TestStepFailed"
  step_value:       "MeasuredValue"
  step_type:        "MeasurementType"
  step_low_limit:   "LowerLimit"
  step_high_limit:  "UpperLimit"
